{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spago build --strict",
    "test": "spago test"
  },
  "devDependencies": {
    "purescript": "^0.15.0",
    "spago": "^0.93.0"
  }
}
