{
  "private": true,
  "scripts": {
    "build": "pulp build -- --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "purescript-psa": "^0.6.0",
    "purescript": "^0.12.0",
    "pulp": "^12.2.0"
  }
}
